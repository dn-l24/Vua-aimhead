-- Đợi game tải xong hoàn toàn dữ liệu người chơi
if not game:IsLoaded() then game.Loaded:Wait() end

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local Workspace = game:GetService("Workspace")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- BIẾN CẤU HÌNH HỆ THỐNG (SETTINGS)
local AimbotEnabled = true
local ESPEnabled = true
local BlueBackgroundEnabled = true -- Mặc định là nền xanh mờ (BẬT)
local MenuVisible = true -- Trạng thái ẩn/hiện của bảng menu chính
local FPSToggleState = false -- Chức năng tăng FPS mặc định là TẮT
local FlyEnabled = false -- Chức năng Bay mặc định là TẮT
local FlySpeed = 50 -- Tốc độ bay mặc định
local SpinEnabled = false -- Chức năng Quay mặc định là TẮT
local FOV_RADIUS = 35 
local MAX_LINE_DISTANCE = 200 
local TARGET_PART = "Head"
local CurrentTarget = nil

-- Cấu hình màu sắc mặc định
local FOVColor = Color3.fromRGB(173, 216, 230) 
local ESPColor = Color3.fromRGB(0, 162, 255)  
local DefaultMenuColor = Color3.fromRGB(30, 30, 35) -- Màu nền xám tối gốc
local BlueMenuColor = Color3.fromRGB(10, 40, 75)   -- Màu nền xanh nước biển mờ

-- DANH SÁCH MÀU SẮC ĐỂ THAY ĐỔI TRÊN MENU
local ColorPresets = {
    {Name = "Cyan", Color = Color3.fromRGB(0, 162, 255)},
    {Name = "Red", Color = Color3.fromRGB(255, 0, 0)},
    {Name = "Green", Color = Color3.fromRGB(0, 255, 0)},
    {Name = "Yellow", Color = Color3.fromRGB(255, 255, 0)},
    {Name = "Purple", Color = Color3.fromRGB(170, 0, 255)},
    {Name = "White", Color = Color3.fromRGB(255, 255, 255)}
}
local currentFOVColorIndex = 1
local currentESPColorIndex = 1

-- KHỞI TẠO DRAWING API
local FOVCircle = Drawing.new("Circle")
FOVCircle.Thickness = 1.5
FOVCircle.Color = FOVColor
FOVCircle.Filled = false
FOVCircle.Radius = FOV_RADIUS
FOVCircle.Visible = AimbotEnabled

-- KHỞI TẠO 3 ĐƯỜNG TRACER LINES CHO 3 KẺ ĐỊCH GẦN NHẤT
local TracerLines = {}
for i = 1, 3 do
    local line = Drawing.new("Line")
    line.Thickness = 1.5
    line.Color = Color3.fromRGB(255, 255, 0)
    line.Transparency = 1
    line.Visible = false
    table.insert(TracerLines, line)
end

-- LOGIC HÀM BỔ TRỢ (UTILITIES)
local function isVisible(targetCharacter, targetPart)
    if not LocalPlayer.Character or not targetCharacter then return false end
    local raycastParams = RaycastParams.new()
    raycastParams.FilterType = Enum.RaycastFilterType.Exclude
    raycastParams.FilterDescendantsInstances = {LocalPlayer.Character, targetCharacter, Camera}
    local origin = Camera.CFrame.Position
    local direction = targetPart.Position - origin
    local raycastResult = workspace:Raycast(origin, direction, raycastParams)
    return raycastResult == nil
end

local function getClosestPlayerToCenter()
    local closestPlayer = nil
    local shortestDistance = math.huge
    local screenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild(TARGET_PART) then
            local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
            if humanoid and humanoid.Health > 0 then
                local targetPart = player.Character[TARGET_PART]
                local screenPos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
                if onScreen then
                    local targetPos2D = Vector2.new(screenPos.X, screenPos.Y)
                    local distanceToCenter = (targetPos2D - screenCenter).Magnitude
                    if distanceToCenter <= FOV_RADIUS and distanceToCenter < shortestDistance then
                        if isVisible(player.Character, targetPart) then
                            closestPlayer = player
                            shortestDistance = distanceToCenter
                        end
                    end
                end
            end
        end
    end
    return closestPlayer
end

local function getTop3ClosestPlayersForTracers()
    local targets = {}
    local localCharacter = LocalPlayer.Character
    local localRoot = localCharacter and localCharacter:FindFirstChild("HumanoidRootPart")
    if not localRoot then return targets end

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild(TARGET_PART) then
            local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
            local rootPart = player.Character:FindFirstChild("HumanoidRootPart")
            if humanoid and humanoid.Health > 0 and rootPart then
                local distance = (rootPart.Position - localRoot.Position).Magnitude
                if distance <= MAX_LINE_DISTANCE then
                    table.insert(targets, {Player = player, Distance = distance})
                end
            end
        end
    end

    table.sort(targets, function(a, b)
        return a.Distance < b.Distance
    end)

    local top3 = {}
    for i = 1, math.min(3, #targets) do
        table.insert(top3, targets[i].Player)
    end
    return top3
end

local function applyESP(player)
    if player == LocalPlayer then return end
    local character = player.Character
    if character then
        local highlight = character:FindFirstChild("ESPHighlight")
        if not highlight then
            highlight = Instance.new("Highlight")
            highlight.Name = "ESPHighlight"
            highlight.FillTransparency = 0.5 
            highlight.OutlineColor = Color3.fromRGB(255, 255, 255) 
            highlight.OutlineTransparency = 0 
            highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop 
            highlight.Parent = character
        end
        highlight.FillColor = ESPColor
        highlight.Enabled = ESPEnabled
    end
end

local function removeESP(player)
    local character = player.Character
    if character then
        local highlight = character:FindFirstChild("ESPHighlight")
        if highlight then highlight:Destroy() end
    end
end

local function clearAllESP()
    for _, player in ipairs(Players:GetPlayers()) do
        removeESP(player)
    end
end

local HiddenPlayers = {}
local function IsImportant(instance)
    local name = string.lower(instance.Name)
    local protectedNames = {"baseplate", "spawnlocation", "floor", "ground", "sàn", "checkpoint", "teleport"}
    for _, protected in ipairs(protectedNames) do
        if string.find(name, protected) then
            return true
        end
    end
    return false
end

local function SmartOptimize(instance)
    if instance:IsA("BasePart") or instance:IsA("MeshPart") then
        instance.Material = Enum.Material.SmoothPlastic
        instance.CastShadow = false
        instance.Reflectance = 0
        if instance:IsA("MeshPart") and not IsImportant(instance) then
            instance.TextureID = ""
        end
    elseif instance:IsA("ParticleEmitter") or instance:IsA("Smoke") or instance:IsA("Fire") or instance:IsA("Sparkles") or instance:IsA("Trail") or instance:IsA("Beam") then
        task.spawn(function()
            if instance and instance.Parent then instance:Destroy() end
        end)
    elseif instance:IsA("Decal") or instance:IsA("Texture") then
        if instance.Parent and not IsImportant(instance.Parent) then
            task.spawn(function()
                if instance and instance.Parent then instance:Destroy() end
            end)
        end
    end
end

local function ExecuteSmartFPSBooster()
    if _G.SmartFPSBoosterExecuted then return end
    _G.SmartFPSBoosterExecuted = true

    Lighting.GlobalShadows = false
    Lighting.Brightness = 1
    Lighting.ClockTime = 12
    for _, effect in ipairs(Lighting:GetChildren()) do
        if effect:IsA("BlurEffect") or effect:IsA("SunRaysEffect") or effect:IsA("BloomEffect") or effect:IsA("DepthOfFieldEffect") then
            effect:Destroy()
        end
    end

    task.spawn(function()
        local count = 0
        for _, descendant in ipairs(Workspace:GetDescendants()) do
            if not descendant:IsDescendantOf(LocalPlayer.Character or workspace) then
                SmartOptimize(descendant)
                count = count + 1
                if count >= 150 then
                    count = 0
                    RunService.Heartbeat:Wait()
                end
            end
        end
    end)

    Workspace.DescendantAdded:Connect(function(descendant)
        if LocalPlayer.Character and descendant:IsDescendantOf(LocalPlayer.Character) then return end
        SmartOptimize(descendant)
    end)

    task.spawn(function()
        while true do
            task.wait(3)
            if FPSToggleState then
                local localCharacter = LocalPlayer.Character
                local localRoot = localCharacter and localCharacter:FindFirstChild("HumanoidRootPart")
                if localRoot then
                    for _, player in ipairs(Players:GetPlayers()) do
                        if player ~= LocalPlayer and player.Character then
                            local char = player.Character
                            local root = char:FindFirstChild("HumanoidRootPart")
                            if root then
                                local distance = (root.Position - localRoot.Position).Magnitude
                                if distance > 250 then
                                    if char.Parent == Workspace then
                                        HiddenPlayers[player] = char
                                        char.Parent = nil
                                    end
                                else
                                    if HiddenPlayers[player] and HiddenPlayers[player].Parent == nil then
                                        HiddenPlayers[player].Parent = Workspace
                                        HiddenPlayers[player] = nil
                                    end
                                end
                            end
                        end
                    end
                end
            else
                for player, char in pairs(HiddenPlayers) do
                    if char and player.Parent then
                        char.Parent = Workspace
                    end
                    HiddenPlayers[player] = nil
                end
            end
            
            for player, char in pairs(HiddenPlayers) do
                if not player.Parent then
                    HiddenPlayers[player] = nil
                end
            end
        end
    end)

    task.spawn(function()
        while true do
            task.wait(20)
            if FPSToggleState then
                gcinfo()
                settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
            end
        end
    end)
end

-- HỆ THỐNG XỬ LÝ FLY (BAY) - ĐÃ FIX LỖI BAY NGƯỢC BIẾN THIÊN THEO CAMERA
local flyConnection = nil
local bodyVelocity = nil
local bodyGyro = nil

local function StartFly()
    if flyConnection then flyConnection:Disconnect() end
    
    local character = LocalPlayer.Character
    local rootPart = character and character:FindFirstChild("HumanoidRootPart")
    local humanoid = character and character:FindFirstChildOfClass("Humanoid")
    
    if not rootPart or not humanoid then return end

    bodyVelocity = Instance.new("BodyVelocity")
    bodyVelocity.Name = "FlyVelocity"
    bodyVelocity.Velocity = Vector3.new(0, 0, 0)
    bodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    bodyVelocity.Parent = rootPart

    bodyGyro = Instance.new("BodyGyro")
    bodyGyro.Name = "FlyGyro"
    bodyGyro.CFrame = rootPart.CFrame
    bodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
    bodyGyro.P = 9000
    bodyGyro.Parent = rootPart

    humanoid.PlatformStand = true

    flyConnection = RunService.RenderStepped:Connect(function()
        if not FlyEnabled or not rootPart or not character.Parent then
            if flyConnection then
                flyConnection:Disconnect()
                flyConnection = nil
            end
            return
        end

        bodyGyro.CFrame = Camera.CFrame
        local walkDir = humanoid.MoveDirection

        if walkDir.Magnitude > 0 then
            -- Áp dụng phép chiếu vector để loại bỏ lỗi đảo ngược trục dựa trên tọa độ thế giới (World Space)
            local camFrame = Camera.CFrame
            local flatCamLook = Vector3.new(camFrame.LookVector.X, 0, camFrame.LookVector.Z).Unit
            local flatCamRight = Vector3.new(camFrame.RightVector.X, 0, camFrame.RightVector.Z).Unit
            
            -- Tính toán tỉ lệ di chuyển tương đối so với góc xoay phẳng của Camera
            local localZ = walkDir:Dot(flatCamLook)
            local localX = walkDir:Dot(flatCamRight)
            
            -- Đồng bộ hóa vector di chuyển 3D thực tế theo đúng Vector Look hướng nhìn của Camera (Fix hoàn toàn bay ngược)
            local flightVector = (camFrame.LookVector * localZ + camFrame.RightVector * localX)
            if flightVector.Magnitude > 0 then
                bodyVelocity.Velocity = flightVector.Unit * FlySpeed
            else
                bodyVelocity.Velocity = Vector3.new(0, 0, 0)
            end
        else
            bodyVelocity.Velocity = Vector3.new(0, 0, 0)
        end
    end)
end

local function StopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    
    local character = LocalPlayer.Character
    local rootPart = character and character:FindFirstChild("HumanoidRootPart")
    local humanoid = character and character:FindFirstChildOfClass("Humanoid")

    if rootPart then
        local fV = rootPart:FindFirstChild("FlyVelocity")
        local fG = rootPart:FindFirstChild("FlyGyro")
        if fV then fV:Destroy() end
        if fG then fG:Destroy() end
    end

    if humanoid then
        humanoid.PlatformStand = false
    end
end

LocalPlayer.CharacterAdded:Connect(function(char)
    if FlyEnabled then
        task.wait(1)
        StartFly()
    end
end)

-- HỆ THỐNG SPIN (QUAY NHÂN VẬT)
local spinConnection = nil
task.spawn(function()
    RunService.Heartbeat:Connect(function()
        if SpinEnabled and not FlyEnabled then
            local character = LocalPlayer.Character
            local rootPart = character and character:FindFirstChild("HumanoidRootPart")
            if rootPart then
                rootPart.CFrame = rootPart.CFrame * CFrame.Angles(0, math.rad(25), 0)
            end
        end
    end)
end)

-- VÒNG LẶP CHÍNH (MAIN LOOP)
RunService.RenderStepped:Connect(function()
    local screenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    
    FOVCircle.Position = screenCenter
    FOVCircle.Radius = FOV_RADIUS
    FOVCircle.Color = FOVColor
    FOVCircle.Visible = AimbotEnabled

    -- LOGIC AIMBOT
    if AimbotEnabled then
        if CurrentTarget and CurrentTarget.Character and CurrentTarget.Character:FindFirstChild(TARGET_PART) then
            local humanoid = CurrentTarget.Character:FindFirstChildOfClass("Humanoid")
            if humanoid and humanoid.Health > 0 then
                local targetPart = CurrentTarget.Character[TARGET_PART]
                local screenPos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
                local targetPos2D = Vector2.new(screenPos.X, screenPos.Y)
                local distanceToCenter = (targetPos2D - screenCenter).Magnitude

                if not onScreen or distanceToCenter > FOV_RADIUS or not isVisible(CurrentTarget.Character, targetPart) then
                    CurrentTarget = nil
                end
            end
        else
            CurrentTarget = nil
        end

        if not CurrentTarget then
            CurrentTarget = getClosestPlayerToCenter()
        end

        if CurrentTarget and CurrentTarget.Character and CurrentTarget.Character:FindFirstChild(TARGET_PART) then
            local targetPos = CurrentTarget.Character[TARGET_PART].Position
            Camera.CFrame = CFrame.lookAt(Camera.CFrame.Position, targetPos)
        end
    else
        CurrentTarget = nil
    end

    -- LOGIC ESP KHỐI & TRACERS LINE
    if ESPEnabled then
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character and player.Character.Parent == Workspace then
                local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
                if humanoid and humanoid.Health > 0 then
                    applyESP(player)
                else
                    removeESP(player)
                end
            end
        end

        local closest3Players = getTop3ClosestPlayersForTracers()
        for i = 1, 3 do
            local targetPlayer = closest3Players[i]
            local line = TracerLines[i]
            
            if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild(TARGET_PART) and targetPlayer.Character.Parent == Workspace then
                local targetPart = targetPlayer.Character[TARGET_PART]
                local screenPos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
                
                if onScreen then
                    line.From = screenCenter
                    line.To = Vector2.new(screenPos.X, screenPos.Y)
                    line.Color = Color3.fromRGB(255, 255, 0)
                    line.Visible = true
                else
                    line.Visible = false
                end
            else
                line.Visible = false
            end
        end
    else
        for i = 1, 3 do
            TracerLines[i].Visible = false
        end
        clearAllESP()
    end
end)

-- Quản lý nạp/tải lại ESP khi nhân vật hồi sinh
local function setupPlayerConnections(player)
    player.CharacterAdded:Connect(function(character) 
        task.wait(0.5) 
        if ESPEnabled and character.Parent == Workspace then 
            applyESP(player) 
        end 
    end)
end

for _, player in ipairs(Players:GetPlayers()) do
    setupPlayerConnections(player)
end

Players.PlayerAdded:Connect(setupPlayerConnections)

Players.PlayerRemoving:Connect(function(player) 
    removeESP(player) 
    if HiddenPlayers[player] then 
        HiddenPlayers[player] = nil 
    end 
end)

-- TẠO GIAO DIỆN HỆ THỐNG MENU KIỂU DASHBOARD
if PlayerGui:FindFirstChild("MobileCheatMenu") then PlayerGui.MobileCheatMenu:Destroy() end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MobileCheatMenu"
ScreenGui.Parent = PlayerGui
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local MainMenu = Instance.new("Frame")
MainMenu.Name = "MainMenu"
MainMenu.Parent = ScreenGui
MainMenu.BorderSizePixel = 0
MainMenu.Position = UDim2.new(0.2, 0, 0.2, 0)
MainMenu.Size = UDim2.new(0, 520, 0, 320)
MainMenu.Active = true
MainMenu.ZIndex = 2

local function updateMenuBackground()
    if BlueBackgroundEnabled then
        MainMenu.BackgroundColor3 = BlueMenuColor
        MainMenu.BackgroundTransparency = 0.2
    else
        MainMenu.BackgroundColor3 = DefaultMenuColor
        MainMenu.BackgroundTransparency = 0
    end
end
updateMenuBackground()

local MenuCorner = Instance.new("UICorner")
MenuCorner.CornerRadius = UDim.new(0, 10)
MenuCorner.Parent = MainMenu

local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.Parent = MainMenu
TopBar.Size = UDim2.new(1, 0, 0, 35)
TopBar.BackgroundTransparency = 1

local Title = Instance.new("TextLabel")
Title.Parent = TopBar
Title.Size = UDim2.new(0, 200, 1, 0)
Title.Position = UDim2.new(0, 15, 0, 0)
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.SourceSansBold
Title.Text = "Aimbot l Vua"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 16
Title.TextXAlignment = Enum.TextXAlignment.Left

local Sidebar = Instance.new("Frame")
Sidebar.Name = "Sidebar"
Sidebar.Parent = MainMenu
Sidebar.Position = UDim2.new(0, 10, 0, 45)
Sidebar.Size = UDim2.new(0, 140, 1, -55)
Sidebar.BackgroundTransparency = 0.9
Sidebar.BackgroundColor3 = Color3.fromRGB(0, 0, 0)

local SidebarCorner = Instance.new("UICorner")
SidebarCorner.CornerRadius = UDim.new(0, 8)
SidebarCorner.Parent = Sidebar

local SidebarLayout = Instance.new("UIListLayout")
SidebarLayout.Parent = Sidebar
SidebarLayout.Padding = UDim.new(0, 5)
SidebarLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
SidebarLayout.SortOrder = Enum.SortOrder.LayoutOrder

local ContentContainer = Instance.new("ScrollingFrame")
ContentContainer.Name = "ContentContainer"
ContentContainer.Parent = MainMenu
ContentContainer.Position = UDim2.new(0, 160, 0, 45)
ContentContainer.Size = UDim2.new(1, -170, 1, -55)
ContentContainer.BackgroundTransparency = 1
ContentContainer.ClipsDescendants = true
ContentContainer.ScrollBarThickness = 4
ContentContainer.ScrollBarImageColor3 = Color3.fromRGB(255, 255, 255)
ContentContainer.CanvasSize = UDim2.new(0, 0, 0, 0)
ContentContainer.ElasticBehavior = Enum.ElasticBehavior.Always
ContentContainer.ScrollingDirection = Enum.ScrollingDirection.Y

local ContentLayout = Instance.new("UIListLayout")
ContentLayout.Parent = ContentContainer
ContentLayout.Padding = UDim.new(0, 8)
ContentLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
ContentLayout.SortOrder = Enum.SortOrder.LayoutOrder

ContentLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    ContentContainer.CanvasSize = UDim2.new(0, 0, 0, ContentLayout.AbsoluteContentSize.Y + 15)
end)

local MenuButton = Instance.new("TextButton")
MenuButton.Name = "MenuVisibilityButton"
MenuButton.Parent = ScreenGui
MenuButton.Size = UDim2.new(0, 50, 0, 50)
MenuButton.Position = UDim2.new(MainMenu.Position.X.Scale, MainMenu.Position.X.Offset - 55, MainMenu.Position.Y.Scale, MainMenu.Position.Y.Offset)
MenuButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
MenuButton.BackgroundTransparency = 1 
MenuButton.Font = Enum.Font.SourceSansBold
MenuButton.Text = "ẨN"
MenuButton.TextColor3 = Color3.fromRGB(255, 255, 255)
MenuButton.TextSize = 14
MenuButton.TextStrokeTransparency = 0.3 
MenuButton.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
MenuButton.ZIndex = 4

local ButtonCorner = Instance.new("UICorner")
ButtonCorner.CornerRadius = UDim.new(0, 8)
ButtonCorner.Parent = MenuButton

local ButtonImage = Instance.new("ImageLabel")
ButtonImage.Name = "ButtonBackground"
ButtonImage.Parent = MenuButton
ButtonImage.Size = UDim2.new(1, 0, 1, 0)
ButtonImage.BackgroundTransparency = 1
ButtonImage.Image = "rbxassetid://YOUR_IMAGE_ID_HERE" 
ButtonImage.ScaleType = Enum.ScaleType.Crop
ButtonImage.ZIndex = 3

local ImageCorner = Instance.new("UICorner")
ImageCorner.CornerRadius = UDim.new(0, 8)
ImageCorner.Parent = ButtonImage

local function createToggle(name, textON, textOFF, defaultState, callback)
    local btn = Instance.new("TextButton")
    local state = defaultState
    btn.Name = name
    btn.Size = UDim2.new(0, 320, 0, 35)
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 14
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = btn

    local function updateView()
        if state then
            btn.Text = textON
            btn.BackgroundColor3 = Color3.fromRGB(0, 170, 100)
        else
            btn.Text = textOFF
            btn.BackgroundColor3 = Color3.fromRGB(170, 50, 50)
        end
    end
    updateView()

    btn.MouseButton1Click:Connect(function()
        state = not state
        updateView()
        callback(state)
    end)
    return btn
end

local function createActionBtn(text, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 320, 0, 32)
    btn.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
    btn.Font = Enum.Font.SourceSansBold
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(220, 220, 220)
    btn.TextSize = 13
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = btn
    
    btn.MouseButton1Click:Connect(callback)
    return btn
end

local Tabs = {Main = {}, Sub = {}}

local function createTabButton(name, layoutOrder)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 125, 0, 35)
    btn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
    btn.Font = Enum.Font.SourceSansBold
    btn.Text = name
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 13
    btn.LayoutOrder = layoutOrder
    btn.Parent = Sidebar

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = btn
    return btn
end

local MainTabBtn = createTabButton("Chức Năng Chính", 1)
local SubTabBtn = createTabButton("Chức Năng Phụ", 2)

local function displayTab(tabName)
    for _, instance in ipairs(ContentContainer:GetChildren()) do
        if instance:IsA("TextButton") then
            instance.Visible = false
        end
    end
    for _, instance in ipairs(Tabs[tabName]) do
        instance.Visible = true
    end
    
    if tabName == "Main" then
        MainTabBtn.BackgroundColor3 = Color3.fromRGB(0, 162, 255)
        SubTabBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
    else
        MainTabBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
        SubTabBtn.BackgroundColor3 = Color3.fromRGB(0, 162, 255)
    end
    ContentContainer.CanvasPosition = Vector2.new(0, 0)
end

-- TAB CHÍNH (ĐÃ CHUYỂN ESP XUYÊN TƯỜNG LÊN ĐÂY)
local AimToggle = createToggle("AimToggle", "AIMBOT: BẬT", "AIMBOT: TẮT", AimbotEnabled, function(v) AimbotEnabled = v end)
AimToggle.Parent = ContentContainer
table.insert(Tabs.Main, AimToggle)

local ESPToggle = createToggle("ESPToggle", "ESP XUYÊN TƯỜNG: BẬT", "ESP XUYÊN TƯỜNG: TẮT", ESPEnabled, function(v) ESPEnabled = v end)
ESPToggle.Parent = ContentContainer
table.insert(Tabs.Main, ESPToggle)

local FPSToggle = createToggle("FPSToggle", "TĂNG FPS: BẬT", "TĂNG FPS: TẮT", FPSToggleState, function(v)
    FPSToggleState = v
    if FPSToggleState then
        ExecuteSmartFPSBooster()
    end
end)
FPSToggle.Parent = ContentContainer
table.insert(Tabs.Main, FPSToggle)

-- TAB PHỤ
local FlyToggle = createToggle("FlyToggle", "BAY (FLY): BẬT", "BAY (FLY): TẮT", FlyEnabled, function(v)
    FlyEnabled = v
    if FlyEnabled then
        StartFly()
    else
        StopFly()
    end
end)
FlyToggle.Parent = ContentContainer
table.insert(Tabs.Sub, FlyToggle)

local FlySpeedBtn
FlySpeedBtn = createActionBtn("TỐC ĐỘ BAY: 50", function()
    if FlySpeed == 50 then FlySpeed = 100
    elseif FlySpeed == 100 then FlySpeed = 150
    elseif FlySpeed == 150 then FlySpeed = 200
    elseif FlySpeed == 200 then FlySpeed = 250
    else FlySpeed = 50 end
    FlySpeedBtn.Text = "TỐC ĐỘ BAY: " .. tostring(FlySpeed)
    if FlyEnabled then StartFly() end
end)
FlySpeedBtn.Parent = ContentContainer
table.insert(Tabs.Sub, FlySpeedBtn)

local SpinToggle = createToggle("SpinToggle", "TỰ QUAY QUANH: BẬT", "TỰ QUAY QUANH: TẮT", SpinEnabled, function(v)
    SpinEnabled = v
end)
SpinToggle.Parent = ContentContainer
table.insert(Tabs.Sub, SpinToggle)

local FOVPlus = createActionBtn("TĂNG BÁN KÍNH FOV (+10)", function()
    FOV_RADIUS = math.clamp(FOV_RADIUS + 10, 10, 300)
end)
FOVPlus.Parent = ContentContainer
table.insert(Tabs.Sub, FOVPlus)

local FOVMinus = createActionBtn("GIẢM BÁN KÍNH FOV (-10)", function()
    FOV_RADIUS = math.clamp(FOV_RADIUS - 10, 10, 300)
end)
FOVMinus.Parent = ContentContainer
table.insert(Tabs.Sub, FOVMinus)

local FOVColorBtn
FOVColorBtn = createActionBtn("MÀU VÒNG TRÒN: Cyan", function()
    currentFOVColorIndex = currentFOVColorIndex + 1
    if currentFOVColorIndex > #ColorPresets then currentFOVColorIndex = 1 end
    FOVColor = ColorPresets[currentFOVColorIndex].Color
    FOVColorBtn.Text = "MÀU VÒNG TRÒN: " .. ColorPresets[currentFOVColorIndex].Name
end)
FOVColorBtn.Parent = ContentContainer
table.insert(Tabs.Sub, FOVColorBtn)

local ESPColorBtn
ESPColorBtn = createActionBtn("MÀU ESP KHỐI: Cyan", function()
    currentESPColorIndex = currentESPColorIndex + 1
    if currentESPColorIndex > #ColorPresets then currentESPColorIndex = 1 end
    ESPColor = ColorPresets[currentESPColorIndex].Color
    if ESPEnabled then
        for _, p in ipairs(Players:GetPlayers()) do applyESP(p) end
    end
    ESPColorBtn.Text = "MÀU ESP KHỐI: " .. ColorPresets[currentESPColorIndex].Name
end)
ESPColorBtn.Parent = ContentContainer
table.insert(Tabs.Sub, ESPColorBtn)

local BackgroundToggle = createToggle("BackgroundToggle", "NỀN MENU: XANH MỜ", "NỀN MENU: MẶC ĐỊNH", BlueBackgroundEnabled, function(v)
    BlueBackgroundEnabled = v
    updateMenuBackground()
end)
BackgroundToggle.Parent = ContentContainer
table.insert(Tabs.Sub, BackgroundToggle)

MainTabBtn.MouseButton1Click:Connect(function() displayTab("Main") end)
SubTabBtn.MouseButton1Click:Connect(function() displayTab("Sub") end)

displayTab("Main")

MenuButton.MouseButton1Click:Connect(function()
    MenuVisible = not MenuVisible
    MainMenu.Visible = MenuVisible
    MenuButton.Text = MenuVisible and "ẨN" or "HIỆN"
end)

-- XỬ LÝ DI CHUYỂN ĐỒNG BỘ (DRAGGABLE) GIỮA MENU VÀ NÚT BẤM
local dragging = false
local dragInput, dragStart, startPos, startBtnPos

local function updateMenuPos(input)
    local delta = input.Position - dragStart
    MainMenu.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    MenuButton.Position = UDim2.new(startBtnPos.X.Scale, startBtnPos.X.Offset + delta.X, startBtnPos.Y.Scale, startBtnPos.Y.Offset + delta.Y)
end

local function handleDrag(guiObject)
    guiObject.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = MainMenu.Position
            startBtnPos = MenuButton.Position
            
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then 
                    dragging = false 
                end
            end)
        end
    end)

    guiObject.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseMovement then
            dragInput = input
        end
    end)
end

handleDrag(MainMenu)
handleDrag(MenuButton)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        updateMenuPos(input)
    end
end)
