-- Đợi game tải xong hoàn toàn dữ liệu người chơi
if not game:IsLoaded() then game.Loaded:Wait() end

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- BIẾN CẤU HÌNH HỆ THỐNG (SETTINGS)
local AimbotEnabled = true
local ESPEnabled = true
local GlassBlurEnabled = false -- LUÔN LUÔN TẮT KÍNH MỜ THEO YÊU CẦU
local MenuVisible = true 
local FOV_RADIUS = 35 
local MAX_LINE_DISTANCE = 200 
local TARGET_PART = "Head"
local CurrentTarget = nil
local CurrentLanguage = "VN" 

-- Cấu hình màu sắc Glassmorphism
local FOVColor = Color3.fromRGB(173, 216, 230) 
local ESPColor = Color3.fromRGB(0, 162, 255)  
local GlassColor = Color3.fromRGB(15, 18, 27)         
local SidebarColor = Color3.fromRGB(10, 12, 18)       
local CardColor = Color3.fromRGB(25, 28, 38)          
local AccentColor = Color3.fromRGB(0, 162, 255)        

-- DANH SÁCH MÀU SẮC ĐỂ THAY ĐỔI TRÊN MENU
local ColorPresets = {
    {Name = "Cyan", Color = Color3.fromRGB(0, 162, 255)},
    {Name = "Red", Color = Color3.fromRGB(255, 0, 0)},
    {Name = "Green", Color = Color3.fromRGB(0, 255, 0)},
    {Name = "Yellow", Color = Color3.fromRGB(255, 255, 0)},
    {Name = "Purple", Color = Color3.fromRGB(170, 0, 255)},
    {Name = "White", Color = Color3.fromRGB(255, 255, 255)}
}
local currentFOVColorIndex = 1
local currentESPColorIndex = 1

-- KHỞI TẠO DRAWING API
local FOVCircle = Drawing.new("Circle")
FOVCircle.Thickness = 1.5
FOVCircle.Color = FOVColor
FOVCircle.Filled = false
FOVCircle.Radius = FOV_RADIUS
FOVCircle.Visible = AimbotEnabled

local TracerLine = Drawing.new("Line")
TracerLine.Thickness = 1.5
TracerLine.Color = Color3.fromRGB(255, 255, 0)
TracerLine.Transparency = 1
TracerLine.Visible = false

-- LOGIC HÀM BỔ TRỢ (UTILITIES)
local function isVisible(targetCharacter, targetPart)
    if not LocalPlayer.Character or not targetCharacter then return false end
    local raycastParams = RaycastParams.new()
    raycastParams.FilterType = Enum.RaycastFilterType.Exclude
    raycastParams.FilterDescendantsInstances = {LocalPlayer.Character, targetCharacter, Camera}
    local origin = Camera.CFrame.Position
    local direction = targetPart.Position - origin
    local raycastResult = workspace:Raycast(origin, direction, raycastParams)
    return raycastResult == nil
end

local function isTeam(player)
    if LocalPlayer.Team and player.Team then return LocalPlayer.Team == player.Team end
    return false
end

local function getClosestPlayerToCenter()
    local closestPlayer = nil
    local shortestDistance = math.huge
    local screenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and not isTeam(player) and player.Character and player.Character:FindFirstChild(TARGET_PART) then
            local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
            if humanoid and humanoid.Health > 0 then
                local targetPart = player.Character[TARGET_PART]
                local screenPos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
                if onScreen then
                    local targetPos2D = Vector2.new(screenPos.X, screenPos.Y)
                    local distanceToCenter = (targetPos2D - screenCenter).Magnitude
                    if distanceToCenter <= FOV_RADIUS and distanceToCenter < shortestDistance then
                        if isVisible(player.Character, targetPart) then
                            closestPlayer = player
                            shortestDistance = distanceToCenter
                        end
                    end
                end
            end
        end
    end
    return closestPlayer
end

local function getClosestPlayerForTracer()
    local closestPlayer = nil
    local shortestDistance = math.huge
    local localCharacter = LocalPlayer.Character
    local localRoot = localCharacter and localCharacter:FindFirstChild("HumanoidRootPart")
    if not localRoot then return nil end

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and not isTeam(player) and player.Character and player.Character:FindFirstChild(TARGET_PART) then
            local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
            local rootPart = player.Character:FindFirstChild("HumanoidRootPart")
            if humanoid and humanoid.Health > 0 and rootPart then
                local distance = (rootPart.Position - localRoot.Position).Magnitude
                if distance <= MAX_LINE_DISTANCE and distance < shortestDistance then
                    closestPlayer = player
                    shortestDistance = distance
                end
            end
        end
    end
    return closestPlayer
end

local function applyESP(player)
    if player == LocalPlayer then return end
    local character = player.Character
    if character then
        local highlight = character:FindFirstChildOfClass("Highlight")
        if not highlight then
            highlight = Instance.new("Highlight")
            highlight.Name = "ESPHighlight"
            highlight.FillTransparency = 0.5 
            highlight.OutlineColor = Color3.fromRGB(255, 255, 255) 
            highlight.OutlineTransparency = 0 
            highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop 
            highlight.Parent = character
        end
        highlight.FillColor = ESPColor
        highlight.Enabled = ESPEnabled
    end
end

local function removeESP(player)
    local character = player.Character
    if character then
        local highlight = character:FindFirstChild("ESPHighlight")
        if highlight then highlight:Destroy() end
    end
end

-- VÒNG LẶP CHÍNH (MAIN LOOP)
local mainConnection = RunService.RenderStepped:Connect(function()
    local screenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    FOVCircle.Position = screenCenter
    FOVCircle.Radius = FOV_RADIUS
    FOVCircle.Color = FOVColor
    FOVCircle.Visible = AimbotEnabled

    if AimbotEnabled then
        if CurrentTarget and CurrentTarget.Character and not isTeam(CurrentTarget) and CurrentTarget.Character:FindFirstChild(TARGET_PART) then
            local humanoid = CurrentTarget.Character:FindFirstChildOfClass("Humanoid")
            if humanoid and humanoid.Health > 0 then
                local targetPart = CurrentTarget.Character[TARGET_PART]
                local screenPos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
                local targetPos2D = Vector2.new(screenPos.X, screenPos.Y)
                local distanceToCenter = (targetPos2D - screenCenter).Magnitude
                if not onScreen or distanceToCenter > FOV_RADIUS or not isVisible(CurrentTarget.Character, targetPart) then
                    CurrentTarget = nil
                end
            else CurrentTarget = nil end
        else CurrentTarget = nil end

        if not CurrentTarget then CurrentTarget = getClosestPlayerToCenter() end
        if CurrentTarget and CurrentTarget.Character and CurrentTarget.Character:FindFirstChild(TARGET_PART) then
            Camera.CFrame = CFrame.lookAt(Camera.CFrame.Position, CurrentTarget.Character[TARGET_PART].Position)
        end
    else CurrentTarget = nil end

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
            if humanoid and humanoid.Health > 0 and not isTeam(player) then applyESP(player) else removeESP(player) end
        end
    end

    if ESPEnabled then
        local targetPlayer = getClosestPlayerForTracer()
        if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild(TARGET_PART) then
            local screenPos, onScreen = Camera:WorldToViewportPoint(targetPlayer.Character[TARGET_PART].Position)
            if onScreen then
                TracerLine.From = screenCenter
                TracerLine.To = Vector2.new(screenPos.X, screenPos.Y)
                TracerLine.Visible = true
            else TracerLine.Visible = false end
        else TracerLine.Visible = false end
    else
        TracerLine.Visible = false
        for _, player in ipairs(Players:GetPlayers()) do removeESP(player) end
    end
end)

for _, player in ipairs(Players:GetPlayers()) do
    player.CharacterAdded:Connect(function() task.wait(0.5) if ESPEnabled then applyESP(player) end end)
end
Players.PlayerAdded:Connect(function(player)
    player.CharacterAdded:Connect(function() task.wait(0.5) if ESPEnabled then applyESP(player) end end)
end)
Players.PlayerRemoving:Connect(function(player) removeESP(player) end)


-- ==================== XÂY DỰNG GIAO DIỆN HỢP NHẤT ĐỒNG BỘ ====================
if PlayerGui:FindFirstChild("MobileCheatMenu") then PlayerGui.MobileCheatMenu:Destroy() end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MobileCheatMenu"
ScreenGui.Parent = PlayerGui
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true 

-- KHUNG BAO CHỨA CHÍNH (Đảm bảo Menu và Nút Ẩn/Hiện luôn đi liền nhau khi di chuyển)
local MasterFrame = Instance.new("Frame")
MasterFrame.Name = "MasterFrame"
MasterFrame.Parent = ScreenGui
MasterFrame.BackgroundTransparency = 1
MasterFrame.Position = UDim2.new(0.3, 0, 0.3, 0)
MasterFrame.Size = UDim2.new(0, 420, 0, 260) -- Kích thước tổng thể

-- Khung Menu Nội Dung Chính
local MainMenu = Instance.new("Frame")
MainMenu.Name = "MainMenu"
MainMenu.Parent = MasterFrame
MainMenu.BorderSizePixel = 0
MainMenu.Position = UDim2.new(0, 0, 0, 0)
MainMenu.Size = UDim2.new(1, 0, 1, 0)
MainMenu.BackgroundColor3 = GlassColor
MainMenu.BackgroundTransparency = 0.15 
MainMenu.Active = true
MainMenu.ZIndex = 2

local MenuCorner = Instance.new("UICorner")
MenuCorner.CornerRadius = UDim.new(0, 10)
MenuCorner.Parent = MainMenu

local MenuStroke = Instance.new("UIStroke")
MenuStroke.Thickness = 1
MenuStroke.Color = Color3.fromRGB(255, 255, 255)
MenuStroke.Transparency = 0.85 
MenuStroke.Parent = MainMenu

-- Khung Tiêu Đề Trên (TopBar) - SỬA TÊN THÀNH VUA AIMHEAD
local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.Parent = MainMenu
TopBar.Size = UDim2.new(1, 0, 0, 35)
TopBar.BackgroundColor3 = SidebarColor
TopBar.BackgroundTransparency = 0.4
TopBar.BorderSizePixel = 0
TopBar.ZIndex = 2

local TopCorner = Instance.new("UICorner")
TopCorner.CornerRadius = UDim.new(0, 10)
TopCorner.Parent = TopBar

local Title = Instance.new("TextLabel")
Title.Parent = TopBar
Title.Size = UDim2.new(0, 200, 1, 0)
Title.Position = UDim2.new(0, 12, 0, 0)
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.SourceSansBold
Title.Text = "👑 VUA AIMHEAD"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 15
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.ZIndex = 3

-- NÚT ẨN/HIỆN ĐỒNG BỘ (Dính chặt vào cạnh trái Menu chính)
local MenuButton = Instance.new("TextButton")
MenuButton.Name = "MenuVisibilityButton"
MenuButton.Parent = MasterFrame
MenuButton.Size = UDim2.new(0, 45, 0, 45)
-- Đặt vị trí bên trái và cách lề Menu 10 pixel
MenuButton.Position = UDim2.new(0, -55, 0, 0) 
MenuButton.BackgroundColor3 = GlassColor
MenuButton.BackgroundTransparency = 0.15
MenuButton.Font = Enum.Font.SourceSansBold
MenuButton.Text = "ẨN"
MenuButton.TextColor3 = Color3.fromRGB(255, 255, 255)
MenuButton.TextSize = 13
MenuButton.ZIndex = 5

local ButtonCorner = Instance.new("UICorner")
ButtonCorner.CornerRadius = UDim.new(0, 12)
ButtonCorner.Parent = MenuButton

local ButtonStroke = Instance.new("UIStroke")
ButtonStroke.Color = AccentColor
ButtonStroke.Thickness = 1.5
ButtonStroke.Parent = MenuButton

-- THANH SIDEBAR BÊN TRÁI 
local Sidebar = Instance.new("Frame")
Sidebar.Name = "Sidebar"
Sidebar.Parent = MainMenu
Sidebar.Position = UDim2.new(0, 0, 0, 35)
Sidebar.Size = UDim2.new(0, 120, 1, -35)
Sidebar.BackgroundColor3 = SidebarColor
Sidebar.BackgroundTransparency = 0.3
Sidebar.BorderSizePixel = 0
Sidebar.ZIndex = 2

local SideCorner = Instance.new("UICorner")
SideCorner.CornerRadius = UDim.new(0, 10)
SideCorner.Parent = Sidebar

-- KHUNG CHỨA NỘI DUNG BÊN PHẢI
local MainContent = Instance.new("Frame")
MainContent.Name = "MainContent"
MainContent.Parent = MainMenu
MainContent.Position = UDim2.new(0, 125, 0, 40)
MainContent.Size = UDim2.new(1, -130, 1, -45)
MainContent.BackgroundTransparency = 1
MainContent.ZIndex = 2

local UI_Elements = {}
local Pages = {}

local function createPage(name)
    local page = Instance.new("ScrollingFrame")
    page.Name = name .. "Page"
    page.Parent = MainContent
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.CanvasSize = UDim2.new(0, 0, 0, 320)
    page.ScrollBarThickness = 2
    page.ScrollBarImageColor3 = Color3.fromRGB(255, 255, 255)
    page.ScrollBarImageTransparency = 0.7
    page.Visible = false
    page.ZIndex = 3
    
    local layout = Instance.new("UIListLayout")
    layout.Parent = page
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    layout.Padding = UDim.new(0, 6)
    
    Pages[name] = page
    return page
end

local MainContentPage = createPage("Main")
local SubContentPage = createPage("Sub")
MainContentPage.Visible = true 

local CurrentTab = "Main"
local function createTabBtn(name, textTable, order)
    local btn = Instance.new("TextButton")
    btn.Parent = Sidebar
    btn.Size = UDim2.new(1, -10, 0, 32)
    btn.Position = UDim2.new(0, 5, 0, 10 + (order * 36))
    btn.BorderSizePixel = 0
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 13
    btn.ZIndex = 3
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 6)
    btnCorner.Parent = btn
    
    local function updateTabStyle()
        btn.Text = (CurrentLanguage == "VN") and textTable.VN or textTable.EN
        if CurrentTab == name then
            btn.BackgroundColor3 = AccentColor
            btn.BackgroundTransparency = 0.2
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        else
            btn.BackgroundColor3 = CardColor
            btn.BackgroundTransparency = 0.6
            btn.TextColor3 = Color3.fromRGB(200, 200, 205)
        end
    end
    
    btn.MouseButton1Click:Connect(function()
        CurrentTab = name
        for _, page in pairs(Pages) do page.Visible = false end
        Pages[name].Visible = true
        for _, func in ipairs(UI_Elements) do func() end
    end)
    
    table.insert(UI_Elements, updateTabStyle)
    updateTabStyle()
end

createTabBtn("Main", {VN = "⚔️ Khóa Tâm", EN = "⚔️ Aimbot"}, 0)
createTabBtn("Sub", {VN = "⚙️ Hệ Thống", EN = "⚙️ System"}, 1)

-- HÀM TẠO TOGGLE GẠT
local function createModernToggle(parentPage, labelTexts, defaultState, callback)
    local container = Instance.new("Frame")
    container.Parent = parentPage
    container.Size = UDim2.new(1, -5, 0, 35)
    container.BackgroundColor3 = CardColor
    container.BackgroundTransparency = 0.4 
    container.BorderSizePixel = 0
    container.ZIndex = 3
    
    local uicorner = Instance.new("UICorner")
    uicorner.CornerRadius = UDim.new(0, 6)
    uicorner.Parent = container
    
    local label = Instance.new("TextLabel")
    label.Parent = container
    label.Size = UDim2.new(0, 160, 1, 0)
    label.Position = UDim2.new(0, 10, 0, 0)
    label.BackgroundTransparency = 1
    label.Font = Enum.Font.SourceSans
    label.TextSize = 13
    label.TextColor3 = Color3.fromRGB(240, 240, 240)
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.ZIndex = 4

    local toggleTrack = Instance.new("TextButton")
    toggleTrack.Parent = container
    toggleTrack.Size = UDim2.new(0, 36, 0, 18)
    toggleTrack.Position = UDim2.new(1, -46, 0, 8)
    toggleTrack.Text = ""
    toggleTrack.ZIndex = 4
    
    local trackCorner = Instance.new("UICorner")
    trackCorner.CornerRadius = UDim.new(0, 9)
    trackCorner.Parent = toggleTrack
    
    local toggleCircle = Instance.new("Frame")
    toggleCircle.Parent = toggleTrack
    toggleCircle.Size = UDim2.new(0, 14, 0, 14)
    toggleCircle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    toggleCircle.BorderSizePixel = 0
    toggleCircle.ZIndex = 5
    
    local circleCorner = Instance.new("UICorner")
    circleCorner.CornerRadius = UDim.new(0, 7)
    circleCorner.Parent = toggleCircle

    local state = defaultState
    local function updateView()
        label.Text = (CurrentLanguage == "VN") and labelTexts.VN or labelTexts.EN
        if state then
            toggleTrack.BackgroundColor3 = AccentColor
            toggleTrack.BackgroundTransparency = 0
            toggleCircle.Position = UDim2.new(1, -16, 0, 2)
        else
            toggleTrack.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
            toggleTrack.BackgroundTransparency = 0.8
            toggleCircle.Position = UDim2.new(0, 2, 0, 2)
        end
    end

    toggleTrack.MouseButton1Click:Connect(function()
        state = not state
        updateView()
        callback(state)
    end)
    
    table.insert(UI_Elements, updateView)
    updateView()
end

-- HÀM TẠO NÚT BẤM ACTION
local function createActionBtn(parentPage, texts, callback)
    local btn = Instance.new("TextButton")
    btn.Parent = parentPage
    btn.Size = UDim2.new(1, -5, 0, 30)
    btn.BackgroundColor3 = CardColor
    btn.BackgroundTransparency = 0.4
    btn.Font = Enum.Font.SourceSans
    btn.TextColor3 = Color3.fromRGB(240, 240, 240)
    btn.TextSize = 13
    btn.ZIndex = 3
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 6)
    btnCorner.Parent = btn
    
    local function updateView()
        if type(texts) == "table" then
            btn.Text = (CurrentLanguage == "VN") and texts.VN or texts.EN
        elseif type(texts) == "function" then
            btn.Text = texts()
        end
    end
    
    btn.MouseButton1Click:Connect(function() callback(btn) end)
    table.insert(UI_Elements, updateView)
    updateView()
end


-- ==================== THIẾT LẬP CÁC CHỨC NĂNG VÀO TAB ====================

-- TAB CHÍNH
createModernToggle(MainContentPage, {VN = "Aimbot Tự Động", EN = "Aimbot System"}, AimbotEnabled, function(v) AimbotEnabled = v end)
createModernToggle(MainContentPage, {VN = "ESP Xuyên Tường", EN = "ESP Wallhack"}, ESPEnabled, function(v) ESPEnabled = v end)

createActionBtn(MainContentPage, {VN = "🎯 Tăng Bán Kính FOV (+10)", EN = "🎯 Increase FOV (+10)"}, function()
    FOV_RADIUS = math.clamp(FOV_RADIUS + 10, 10, 300)
end)
createActionBtn(MainContentPage, {VN = "🎯 Giảm Bán Kính FOV (-10)", EN = "🎯 Decrease FOV (-10)"}, function()
    FOV_RADIUS = math.clamp(FOV_RADIUS - 10, 10, 300)
end)

local FOVColorBtn
FOVColorBtn = createActionBtn(MainContentPage, function()
    local name = ColorPresets[currentFOVColorIndex].Name
    return (CurrentLanguage == "VN") and ("🎨 Đổi Màu FOV: " .. name) or ("🎨 FOV Color: " .. name)
end, function()
    currentFOVColorIndex = currentFOVColorIndex + 1
    if currentFOVColorIndex > #ColorPresets then currentFOVColorIndex = 1 end
    FOVColor = ColorPresets[currentFOVColorIndex].Color
    for _, func in ipairs(UI_Elements) do func() end
end)

local ESPColorBtn
ESPColorBtn = createActionBtn(MainContentPage, function()
    local name = ColorPresets[currentESPColorIndex].Name
    return (CurrentLanguage == "VN") and ("🎨 Đổi Màu ESP: " .. name) or ("🎨 ESP Color: " .. name)
end, function()
    currentESPColorIndex = currentESPColorIndex + 1
    if currentESPColorIndex > #ColorPresets then currentESPColorIndex = 1 end
    ESPColor = ColorPresets[currentESPColorIndex].Color
    for _, func in ipairs(UI_Elements) do func() end
end)

-- TAB HỆ THỐNG - THÊM TÍNH NĂNG BOOST FPS XÓA 40% HIỆU ỨNG
createActionBtn(SubContentPage, {VN = "🧹 Siêu Tối Ưu: FPS Boost (Xóa 40% Hiệu Ứng)", EN = "🧹 FPS Boost (Clear 40% Effects)"}, function()
    -- Cài đặt cấu hình đồ họa xuống mức thấp nhất
    UserSettings():GetService("GameSettings").GraphicsQualityLevel = 1
    settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
    
    -- Vòng lặp quét xóa sạch 40% hiệu ứng rác trong không gian game
    local clearedCount = 0
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("ParticleEmitter") or obj:IsA("Smoke") or obj:IsA("Sparkles") or obj:IsA("Fire") then
            clearedCount = clearedCount + 1
            if clearedCount % 10 <= 4 then -- Xóa chuẩn xác tỷ lệ khoảng 40% hiệu ứng hạt nhằm tránh lỗi hiển thị skill
                obj:Destroy()
            end
        elseif obj:IsA("PostEffect") or obj:IsA("BloomEffect") or obj:IsA("DepthOfFieldEffect") then
            obj:Destroy() -- Xóa các bộ lọc xử lý ánh sáng nặng
        end
    end
    print("FPS Boost Activated! Cleared heavy game effects.")
end)

createActionBtn(SubContentPage, function()
    return (CurrentLanguage == "VN") and "🌐 Ngôn Ngữ: Tiếng Việt" or "🌐 Language: English"
end, function()
    CurrentLanguage = (CurrentLanguage == "VN") and "EN" or "VN"
    MenuButton.Text = MenuVisible and ((CurrentLanguage == "VN") and "ẨN" or "HIDE") or ((CurrentLanguage == "VN") and "HIỆN" or "SHOW")
    for _, func in ipairs(UI_Elements) do func() end
end)


-- ==================== XỬ LÝ ĐÓNG MỞ VÀ DI CHUYỂN HỢP NHẤT ====================
-- Đồng bộ nút ẩn hiện ban đầu
MenuButton.Text = MenuVisible and ((CurrentLanguage == "VN") and "ẨN" or "HIDE") or ((CurrentLanguage == "VN") and "HIỆN" or "SHOW")

MenuButton.MouseButton1Click:Connect(function()
    MenuVisible = not MenuVisible
    MainMenu.Visible = MenuVisible
    MenuButton.Text = MenuVisible and ((CurrentLanguage == "VN") and "ẨN" or "HIDE") or ((CurrentLanguage == "VN") and "HIỆN" or "SHOW")
end)

local dragging = false
local dragInput, dragStart, startPos

local function updateMenuPos(input)
    local delta = input.Position - dragStart
    -- Di chuyển toàn bộ MasterFrame chứa cả Menu và Nút đi cùng nhau
    MasterFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

TopBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = MasterFrame.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end)

TopBar.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseMovement then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then updateMenuPos(input) end
end)
